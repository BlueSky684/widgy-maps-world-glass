name: Build v141 high-res emissive atlas

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - tools/prepare-night-v141.py
      - .github/workflows/build-v141-night.yml

permissions:
  contents: write

jobs:
  build-night-atlas:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install builder dependencies
        run: python -m pip install --disable-pip-version-check numpy pillow scipy
      - name: Download untouched NASA Black Marble source
        run: |
          mkdir -p work/map
          curl -L --fail --retry 3 \
            -o work/map/BlackMarble_2016_3km.jpg \
            'https://assets.science.nasa.gov/content/dam/science/esd/eo/images/imagerecords/144000/144898/BlackMarble_2016_3km.jpg'
      - name: Build v141 high-res emissive atlas from scratch
        run: python tools/prepare-night-v141.py
      - name: Commit generated v141 atlas
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add assets/earth/night-network-v141.bin.gz assets/earth/night-body-v141.bin.gz assets/earth/night-core-v141.bin.gz assets/earth/night-provenance-v141.json
          if git diff --cached --quiet; then
            echo 'Generated files are unchanged.'
            exit 0
          fi
          git commit -m 'Build v141 high-res emissive atlas'
          git pull --rebase origin main
          git push
